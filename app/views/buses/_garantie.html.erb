<div class="tab">
  <div class="panel panel-default">
    <!-- Default panel contents
    <div class="panel-heading">Bus en stock</div>-->
    <!-- Table -->
    <table class="table">
      <thead>
        <tr>
          <th>Mention Garantie du Bdc</th>
          <th>Enregistrée dans PEP Tool</th>
        </tr>
      </thead>
      <tbody>
        <%= simple_form_for @line do |f| %>
        <tr>
          <td><%= f.input :mention_garantie, label: false %></td>
          <td><%= f.input :garantie_pep_tool, as: :boolean, label: false %></td>
        </tr>
      </tbody>
    </table>
    <button type="submit" class="btn btn-primary">
    <i class="far fa-save"></i>
    </button>
    <% end %>
  </div>
</div>
