<div class="tab">
  <div class="panel panel-default">
    <!-- Default panel contents-->
    <div class="panel-heading">
      <!-- Table -->
      <table class="table">
        <thead>
          <tr>
            <th>Acheté au client</th>
            <th>Prix d'achat</th>
            <th>Date de rentrée prévue sur le bon de commande</th>
            <th>Date exacte de rentrée</th>
            <th>Heure exacte de rentrée</th>
            <th>action</th>
          </tr>
        </thead>
        <tbody>
          <tr>
            <td><%= @client_name %></td>
            <td><%= @trade.prix_achat %></td>
            <td><%= @trade.date_rentree_bdc%></td>
            <%= simple_form_for @trade do |f| %>
            <td> <%= f.input :date_rentree, as: :date, html5: true, label: false %></td>
            <td><%= f.input :heure_rentree, label: false %> </td>
            <td><button type="submit" class="btn btn-primary">
      <i class="far fa-edit"></i>
      <% end %></td>
          </tr>
        </tbody>
      </table>

    </div>
  </div>
</div>
